import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
  },
  innerContainer: {
    width: '90%',
    marginTop: '10%',
  },
  title: {
    fontSize: 36,
  },
  header: {
    fontSize: 22,
  },
});

export default styles;
